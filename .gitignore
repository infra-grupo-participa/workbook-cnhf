node_modules/
# dist/ É versionado de propósito: a Hostinger Node app às vezes reinicia o
# processo sem o build no lugar, mostrando "preparando...". Com dist/ no repo,
# ele sempre existe no runtime. Rebuildar e committar o dist junto a cada mudança.
.DS_Store
# nunca commitar credenciais de FTP/Supabase
.env
.env.local
# Edge Functions locais contêm chaves de API (ex.: SendFlow) — NÃO versionar
supabase/
